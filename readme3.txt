tensorflow

----------
비니지스 로직 - service 
processing하는 파일명
preprocessing
modeling
learning
evaluation
완성되면 submit (파일로 저장)

외부에 있는 파이썬 파일을 import 해야 속성, 기능을 하용할 수 있다
내부에서는 이것을 인스턴스화 해야한다
entity = Entity()
이때 소문자 entty는 인스턴스라고 하고 이를 객체로 정의한다
대문자 Entity는 클래스
라운드 브레이스가 있는 Entity()는 생성자라고 한다
결론.. 객체지향에서는 속성과 기능을 호출하sms 구조로 두가지 방식이 있는데
calc = Calculator